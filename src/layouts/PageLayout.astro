---
import BaseLayout from './BaseLayout.astro';
import PageIntro from '../components/intro/PageIntro.astro';
import type { SiteNavDepth, SiteNavSection } from '../components/navigation/SiteNav.astro';

interface Props {
  title: string;
  description: string;
  documentTitle?: string;
  intro?: string;
  showTitle?: boolean;
  class?: string;
  noindex?: boolean;
  showHeader?: boolean;
  showFooter?: boolean;
  bodyClass?: string;
  siteNavSection?: SiteNavSection;
  siteNavDepth?: SiteNavDepth;
}

const {
  title,
  description,
  documentTitle = `${title} Бориса Евдеева`,
  intro,
  showTitle = true,
  class: className,
  noindex = false,
  showHeader = true,
  showFooter = true,
  bodyClass,
  siteNavSection,
  siteNavDepth = 'section',
} = Astro.props;
---

<BaseLayout
  documentTitle={documentTitle}
  description={description}
  noindex={noindex}
  showHeader={showHeader}
  showFooter={showFooter}
  bodyClass={bodyClass}
  siteNavSection={siteNavSection}
  siteNavDepth={siteNavDepth}
>
  <article class:list={['content', 'page-content', className]}>
    {showTitle && <PageIntro title={title} description={intro} />}
    <slot />
  </article>
</BaseLayout>
