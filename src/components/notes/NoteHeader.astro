---
import { typografText } from '../../lib/typography.mjs';

interface Props {
  title: string;
  description: string;
  intro?: string;
  created: Date;
  tags: string[];
}

const { title, description, intro, created, tags } = Astro.props;
const displayTitle = typografText(title);
const formattedDate = new Intl.DateTimeFormat('ru-RU', {
  day: 'numeric',
  month: 'long',
  year: 'numeric',
}).format(created);
---

<header class="note-header">
  <p class="note-date"><time datetime={created.toISOString()}>{formattedDate}</time></p>
  <h1>{displayTitle}</h1>
  <div class="article-lead">
    <p>{typografText(intro ?? description)}</p>
  </div>
  {tags.length > 0 && <p class="note-tags">{tags.map((tag) => `#${tag}`).join(' ')}</p>}
</header>
